# arweave-bundler-server

This allows you to run a bundling server. Make sure that `arLocal` is started and the key file you use is funded.

Start the bundling server using:

`npm start <path-to-keyfile>`
